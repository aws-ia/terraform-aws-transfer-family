# Main configuration file
# Stage-specific resources are defined in stage*.tf files
# Use terraform apply -var-file=stageN.tfvars to enable specific stages

terraform {
  required_version = ">= 1.0"
}
