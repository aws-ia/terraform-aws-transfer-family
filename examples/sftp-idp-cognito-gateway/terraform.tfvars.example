# AWS Configuration
aws_region = "us-east-1"

# Cognito Configuration
cognito_user_pool_name   = "my-sftp-users"
cognito_user_pool_client = "my-sftp-client"

# S3 Configuration
bucket_prefix = "my-company"

# Resource Tags
tags = {
  Environment = "production"
  Project     = "sftp-server"
  Owner       = "platform-team"
}
