build/
plan.out
plan.out.json

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sentitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
#
*.tfvars

# Exclude all .pem files, which are likely sensitive security keys to log into your server.
#
*.pem

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
#
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc
.terraform.lock.hcl

# Terratest / GoLang 
go.mod
go.sum

# Terraform tests 
tests/*.auto.tfvars

checkov_reports/

tf_security_scan.sh


# Lambda layer artifacts and build directories
**/lambda-layer.zip
**/lambda-function.zip
**/layer/
**/build/

# Additional Terraform state and plan files
*.tfplan
*.tfplan.*
.terraform.tfstate.lock.info

# Terraform module cache
.terraform.d/

# Ignore macOS system files
**/.DS_Store

# Kiro IDE and sensitive configuration directories
.kiro/
.vscode/
.idea/
.fleet/
.cursor/

# Environment and configuration files that may contain sensitive data
.env
.env.*
!.env.example
.envrc
.secrets
.credentials

# IDE and editor specific files
*.swp
*.swo
*~
.vimrc.local
.nvimrc.local

# OS specific files
Thumbs.db
ehthumbs.db
Desktop.ini

# Temporary and cache directories
.cache/
.tmp/
temp/
tmp/

# Log files
*.log
logs/

# Backup files
*.bak
*.backup
*.orig

# Archive files (often contain sensitive data)
*.zip
*.tar
*.tar.gz
*.tgz
*.rar
*.7z

# SSH keys and certificates (extra protection)
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.key
*.crt
*.csr
*.p12
*.pfx

# AWS and cloud provider credentials
.aws/credentials
.aws/config
.azure/
.gcp/

# Docker and container files that might contain secrets
.dockerignore
docker-compose.override.yml

# Package manager directories
node_modules/
.npm/
.yarn/
.pnpm/

# Python virtual environments and cache
venv/
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Ruby
.bundle/
vendor/

# Go
vendor/

# Keep .tfvars.example files for reference but ignore actual .tfvars
*.tfvars
!*.tfvars.example

# Additional Terraform directories and files
.terraform/
**/.terraform/

# Documentation directory (from main branch)
docs/
modules/custom-idps/lambda-layer/
